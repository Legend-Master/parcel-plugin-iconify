{
    "name": "parcel-iconify",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "build": "tsc --build",
        "watch": "tsc --build --watch",
        "publish": "npm run build && cd ./packages/parcel-resolver-iconify && npm publish && cd ../parcel-transformer-iconify && npm publish"
    }
}